extends ../layout/index
block contenido 

    div.py-10
        h1.text-4xl.my-10.font-extrabold.text-center.text-white Find
            span.font-normal Job
        h2.text-center.text-2xl.font-extrabold.text-white=page 

        if errores
            div(class="max-w-md mx-auto my-10")
                each error in errores
                      p(class="bg-red-600 text-white uppercase text-xs text-center p-2 mb-1 font-bold")= error.msg
        div.mt-8.mx-auto.max-w-md 
            div.bg-white.py-8.px-4.shadow
             form.space-y-5(method="POST")
                
                input(type="hidden" name="_csrf" value=csrfToken)
                
                div 
                    label.block.text-sm.uppercase.text-gray-800.mb-2.font-bold(for="password") You put new change
                    input#email.w-full.px-3.py-2.border.border-gray-300.rounded-md.placeholder-gray-400(placeholder="Your new password" type="password" name="password")    
        
               
                input(class ="w-full bg-blue-600 hover:bg-blue-700 text-white font-bold py-3 cursor-pointer" type="submit" value="Sent password")

